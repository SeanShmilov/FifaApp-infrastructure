apiVersion: v1
kind: Service
metadata:
  name: fraudsterslist-frontend-service
  namespace: fraudsterslist
spec:
  selector:
    app: fraudsterslist-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
